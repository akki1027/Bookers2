<body>
	<div class="container">
		<div class="row">
			<%= render 'books/list', book: @book, user: @user %>

			<!-- 右側のテーブル -->
			<div class="col-xs-9">
				<h2>Users</h2>
				<table class="table table-hover table-inverse">
					<thead>
						<tr>
							<th>image</th>
							<th>name</th>
							<th><!-- 空白 --></th>
							<th colspan="3"></th>
						</tr>
					</thead>
<!-- 					<tbody>
							<tr>
								<td><%= attachment_image_tag @user, :profile_image, :fill, 50, 50, format: 'jpeg', class: "img-square pull-left profile-img", fallback: "no_image.jpg", size: "40x40" %></td>
								<td><%= @user.name %></td>
								<td><% user ||= current_user %>
									<div class="stats" id="followercount_<%= @user.id %>">
										<%= render 'users/followercount', user: @user %>
									</div>
								</td>
								<td><div class="stats" id="followedcount_<%= @user.id %>">
										<%= render 'users/followedcount', user: @user %>
									</div>
								</td>
								<td id="follow_<%= @user.id %>">
									<%= render 'users/follow', user: @user %>
								</td>
								<td><%= link_to "Show", user_path(@user) %></td>
							</tr>
					</tbody> -->
					<% @users.each do |user| %>
						<tbody>
							<tr>
								<td><%= attachment_image_tag user, :profile_image, :fill, 50, 50, format: 'jpeg', class: "img-square pull-left profile-img", fallback: "no_image.jpg", size: "40x40" %></td>
								<td><%= user.name %></td>
								<td><% user ||= current_user %>
									<div class="stats" id="followercount_<%= user.id %>">
										<%= render 'users/followercount', user: user %>
									</div>
								</td>
								<td><div class="stats" id="followedcount_<%= user.id %>">
										<%= render 'users/followedcount', user: user %>
									</div>
								</td>
								<td id="follow_<%= user.id %>">
									<%= render 'users/follow', user: user %>
								</td>
								<td><%= link_to "Show", user_path(user) %></td>
							</tr>
						</tbody>
					<% end %>
				</table>
			</div>
			<!-- ここまで右側のテーブル -->
		</div>
	</div>
</body>

<!-- 全てのuserが表示されるページ -->
<!-- 完成 -->